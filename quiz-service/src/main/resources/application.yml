spring:
  application:
    name: quiz-service
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/quiz-service-db}
    username: ${POSTGRES_USER:surro}
    password: ${POSTGRES_PASSWORD:secret}
    driver-class-name: org.postgresql.Driver
server:
  port: 8090
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true
